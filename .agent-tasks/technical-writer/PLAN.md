# Scope of README.md Documentation Updates

Document recent changes made in the session to ensure README.md matches the actual repository state.

## Core Scope:
1. **TDD Workflow**: Highlight the new Test-Driven Development (TDD) execution loops and agent-facing reference document (`agents/docs/tdd_workflow_agents_reference.md`). Mention that test folders are locked for developer agents, allowing only the `@tester` sub-agent exclusive edit permissions.
2. **Global Skills**:
   * Add `caveman` (ultra-compressed communication mode).
   * Add `jupytext-notebooks` (specialized Jupyter notebooks capability).
   * Add `terraform-test-writer` (Terraform test scenarios).
3. **Ecosystem & Setup**:
   * Document `plugins/compaction-backup.js` under setup components.
   * Document `ua/` (user-agent customization backend) in the repository overview.
4. **Roadmap**:
   * Update roadmap to reflect implemented skills (e.g. moving Jupyter Notebooks from upcoming to active).
