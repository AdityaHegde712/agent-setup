---
description: Resolve decision fatigue and task paralysis on a set of comma-separated tasks
---
You are executing the `/task-paralysis` command.

Load and apply the `task-paralysis-break` skill to the following comma-separated tasks provided as arguments:

$ARGUMENTS

Parse these tasks (assuming they are strictly separated by commas), determine priorities, sequence them if necessary, and output your decisive choice and rationale as instructed by the skill.
